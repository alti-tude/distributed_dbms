class Aggregation:
    NONE = "none"
    MAX = "max"

class PredicateOps:
    AND = "and"
    OR = "or"
    EQ = "eq"

class Keywords:
    SELECT = "select"
    FROM = "from"
    WHERE = "where"
    GROUPBY = "groupby"
    HAVING = "having"
