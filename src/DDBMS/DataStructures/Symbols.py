class Aggregation:
    NONE = 0
    MAX = 1

class PredicateOps:
    AND = "and"
    OR = "or"
    EQ = "eq"

class Keywords:
    SELECT = "select"
    FROM = "from"
    WHERE = "where"
    GROUPBY = "groupby"
    HAVING = "having"
