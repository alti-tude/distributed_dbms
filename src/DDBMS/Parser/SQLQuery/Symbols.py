#TODO modify all these according to moz-sql
class Aggregation:
    NONE = "none"
    MAX = "max"

class PredicateOps:
    AND = "and"
    OR = "or"
    EQ = "eq"
    GEQ = "geq"
    GT = "gt"
    LEQ = "leq"
    LT = "lt"
    NEQ = "neq"
    NOT = "not"

class Keywords:
    SELECT = "select"
    FROM = "from"
    WHERE = "where"
    GROUPBY = "groupby"
    HAVING = "having"
